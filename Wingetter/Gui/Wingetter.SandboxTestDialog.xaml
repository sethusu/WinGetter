<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Test in Windows Sandbox"
        Height="560" Width="720"
        MinHeight="480" MinWidth="640"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResizeWithGrip"
        Background="#F5F7FA">
  <Grid Margin="16">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>

    <TextBlock Grid.Row="0" Text="Validate install, detection, and uninstall" FontSize="18" FontWeight="SemiBold"/>
    <TextBlock Grid.Row="1" x:Name="PackageSummaryText" Margin="0,8,0,8" Foreground="#5C6B7A" TextWrapping="Wrap"/>
    <TextBlock Grid.Row="2" x:Name="StatusText" Margin="0,0,0,10" Foreground="#1B2A41" TextWrapping="Wrap"
               Text="Starting Windows Sandbox..."/>

    <Grid Grid.Row="3">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="1.1*"/>
        <ColumnDefinition Width="1.4*"/>
      </Grid.ColumnDefinitions>

      <Border Grid.Column="0" Background="White" CornerRadius="8" BorderBrush="#D8DEE9" BorderThickness="1" Padding="12" Margin="0,0,8,0">
        <ListBox x:Name="StepList" BorderThickness="0" Background="Transparent">
          <ListBox.ItemTemplate>
            <DataTemplate>
              <StackPanel Margin="0,6">
                <StackPanel Orientation="Horizontal">
                  <TextBlock Text="{Binding Icon}" Width="22" FontFamily="Segoe UI Symbol"/>
                  <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                </StackPanel>
                <TextBlock Text="{Binding Detail}" Margin="22,2,0,0" Foreground="#5C6B7A" TextWrapping="Wrap"/>
              </StackPanel>
            </DataTemplate>
          </ListBox.ItemTemplate>
        </ListBox>
      </Border>

      <Border Grid.Column="1" Background="White" CornerRadius="8" BorderBrush="#D8DEE9" BorderThickness="1" Padding="12">
        <Grid>
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
          </Grid.RowDefinitions>
          <TextBlock Grid.Row="0" Text="Sandbox log" FontWeight="SemiBold" Margin="0,0,0,8"/>
          <TextBox Grid.Row="1" x:Name="LogTextBox" IsReadOnly="True" BorderThickness="0" Background="Transparent"
                   TextWrapping="Wrap" FontFamily="Consolas" FontSize="12" VerticalScrollBarVisibility="Auto"/>
        </Grid>
      </Border>
    </Grid>

    <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,14,0,0">
      <Button x:Name="CancelButton" Content="Cancel" Width="100" Height="32" Margin="0,0,8,0"/>
      <Button x:Name="FailButton" Content="Step failed" Width="110" Height="32" Margin="0,0,8,0" IsEnabled="False"/>
      <Button x:Name="ConfirmButton" Content="Confirm this step" Width="150" Height="32" FontWeight="SemiBold" IsEnabled="False"/>
    </StackPanel>
  </Grid>
</Window>
